-- NOTE: Generated by `pnpm db:generate` because we mirror `neon_auth.account`
-- in `db/schema.ts` (read-only join target). Neon Auth owns that table in
-- production, so this CREATE TABLE would conflict with the live schema. The
-- generated statement has been stripped — same pattern as 0001 for
-- `neon_auth.user`. The migration body is intentionally empty; the side
-- effect we wanted is the updated snapshot in `meta/0002_snapshot.json`,
-- which now records `neon_auth.account` so future `pnpm db:generate` runs
-- are no-ops for it.
--
-- DO NOT add CREATE TABLE / CREATE SCHEMA neon_auth.* statements here.
